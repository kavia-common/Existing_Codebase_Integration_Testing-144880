# Environment configuration for Existing_Codebase_Integration_Testing-144880 backend

# Port to listen on (defaults to 3001 if not set)
PORT=3001

# Node environment: development | production | test
NODE_ENV=development
